# Please edit the object below. Lines beginning with a '#' will be ignored,
# and an empty file will abort the edit. If an error occurs while saving this file will be
# reopened with the relevant failures.
#
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    acme.cert-manager.io/http01-edit-in-place: "true"
    alb.ingress.kubernetes.io/group.name: roboshop
    alb.ingress.kubernetes.io/load-balancer-attributes: access_logs.s3.enabled=true,access_logs.s3.bucket=roboshop-awslogs-demo,access_logs.s3.prefix=eks
    alb.ingress.kubernetes.io/scheme: internet-facing
    alb.ingress.kubernetes.io/target-type: ip
    cert-manager.io/issuer: gitlab-issuer
    kubernetes.io/ingress.provider: nginx
    meta.helm.sh/release-name: gitlab
    meta.helm.sh/release-namespace: gitlab
    nginx.ingress.kubernetes.io/proxy-body-size: 512m
    nginx.ingress.kubernetes.io/proxy-connect-timeout: "15"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "600"
    nginx.ingress.kubernetes.io/service-upstream: "true"
  finalizers:
  - group.ingress.k8s.aws/roboshop
  generation: 2
  labels:
    app: webservice
    app.kubernetes.io/managed-by: Helm
    chart: webservice-7.8.2
    gitlab.com/webservice-name: default
    heritage: Helm
    release: gitlab
  name: gitlab-webservice-default
  namespace: gitlab
  resourceVersion: "462488"
  uid: 39d9d64a-a62c-4c15-9d3b-4d6184f00007
spec:
  ingressClassName: alb
  rules:
  - host: gitlab.decodedevops.com
    http:
      paths:
      - backend:
          service:
            name: cm-acme-http-solver-d5khp
            port:
              number: 8089
        path: /.well-known/acme-challenge/WUl1KD5_hlU7lyMM10W93numGJ9rM6Xo-IFDkdff8UY
        pathType: ImplementationSpecific
      - backend:
          service:
            name: gitlab-webservice-default
            port:
              number: 8181
        path: /
        pathType: Prefix
  tls:
  - hosts:
    - gitlab.decodedevops.com
    secretName: gitlab-gitlab-tls